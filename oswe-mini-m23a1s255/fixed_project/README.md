# E-Commerce Order Filter — Fixed (Safari CSS)  

This repository is a fixed copy of the original demo that resolves the Safari `::-webkit-calendar-picker-indicator` CSS compatibility issue. The fix preserves the original look & feel while restoring the native date-picker functionality in Safari.

See `FIX_SUMMARY.md` for a concise description of the changes and test results.

To run:

```powershell
pip install -r requirements.txt; pytest -q
python src/app.py
```
